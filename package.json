{
  "name": "with-local-tmp-dir",
  "version": "2.2.0",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "repository": "https://github.com/dword-design/with-local-tmp-dir",
  "author": "Sebastian Landwehr <info@dword-design.de>",
  "license": "MIT",
  "description": "Creates a temporary folder inside CWD and passes the path to a function. Afterwards, the folder is removed. Especially useful for testing.",
  "keywords": [
    "test",
    "temp",
    "tmp",
    "temporary",
    "directory",
    "path",
    "cwd",
    "local",
    "remove",
    "cleanup",
    "function",
    "with"
  ],
  "scripts": {
    "prepare": "dw-config-files && mos",
    "pretest": "npm run prepare && install-self",
    "test": "nyc --reporter lcov --reporter text mocha-per-file && mos test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  },
  "dependencies": {
    "chdir": "dword-design/node-chdir#fork",
    "tmp-promise": "^2.0.2"
  },
  "devDependencies": {
    "@dword-design/config-files": "^1.1.1",
    "expect": "^24.9.0",
    "fs-extra": "^8.1.0",
    "husky": "^3.0.9",
    "install-self": "dword-design/install-self#fork",
    "mocha-per-file": "^1.1.3",
    "mos": "dword-design/mos#fork/mos",
    "nyc": "^14.1.1"
  }
}
